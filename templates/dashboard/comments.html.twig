{% extends 'dashboard/base.html.twig' %}

{% block title %}{{ controller_name }}{% endblock %}

{% block body %}


<div class="uk-container">

  <div class="uk-margin-medium-top">

    <h6 class="admin-title">VALORACIONES</h6>
    <h1 class="admin-title-section">Calificaciones</h1>
    <p class="admin-description">Valoraciones de parte de los usuarios.</p>

<div class="uk-overflow-auto">
    <table class="uk-table uk-table-hover uk-table-middle uk-table-divider voucher-table">
    <thead>
        <tr>
            <th>Alias</th>
            <th>Valoración</th>
            <th>Mostrar</th>
        </tr>
    </thead>
    <tbody>

{% for item in rates %}

   <tr>
       <td> {{ item.name }} </td>
       <td> {{ item.message }} </td>
       <td> <a href="{{ path('dashboard-rate-comments-toggle', {'id': item.id}) }}"> {% if item.isvisible  == 1 %} Activado {% else %} Desactivado {% endif %} </a> </td>
   </tr>

{% endfor %}

    </tbody>
</table>
  </div>
  </div>

</div>

{% endblock %}
